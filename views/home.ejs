<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Video Game Inventory</title>
  </head>
  <body>
    <h1>Welcome to the Video Game Store</h1>
    <h2>Browse by Genre</h2>

    <% if (genres.length > 0) { %>
    <ul>
      <% genres.forEach(genre => { %>
      <li>
        <a href="/genres/<%= genre.genre_id %>"> <%= genre.name %> </a>
      </li>
      <% }) %>
    </ul>
    <% } else { %>
    <p>No genres available.</p>
    <% } %>

    <hr />
    <p>
      <a href="/genres">Manage Genres</a> |
      <a href="/video-games">Manage Video Games</a> |
      <a href="/developers">Manage Developers</a>
    </p>
  </body>
</html>
